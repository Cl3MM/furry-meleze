%h1.underline
  = fa_icon "cloud-upload"
  Importer une nouvelle liste de producteurs
= form_tag producteurs_upload_path, multipart: true, role: "form" do
  .row
    =# cadre 1
    .col-md-6.col-md-offset-3
      .panel.panel-default
        .panel-heading
          %h3.panel-title
            = fa_icon "file-text ccc"
            Sélectionnez un fichier Excel à importer
        .panel-body
          = file_field_tag :file
          %p.help-block Le fichier doit être du type Excel ou CSV (.xls, .xlsx ou .csv)
          =# image_submit_tag fa_icon("arrow-circle-right lg", text: "Etape suivante"), class: "btn btn-warning margin-top-30 pull-right"
          = content_tag :button, type: :submit, class: "btn btn-warning margin-top-30 pull-right" do
            Etape suivante&nbsp;
            = fa_icon "arrow-circle-right lg"

